// The map reads the placed coordinates at runtime and uses browser APIs, so it is rendered
// on the client and served by the `fallback: 'index.html'` in svelte.config.js.
export const prerender = false;
