node_modules
/build
/.svelte-kit
/package
.env
.env.*
!.env.example

# Ignore files for PNPM, NPM and YARN
pnpm-lock.yaml
package-lock.json
# The gazetteer seed is a curated data table: one place per line keeps it
# reviewable and editable by hand. Reflowing it to printWidth triples its
# length and makes the place list far harder to scan.
functions/src/gazetteer/seed.ts

# Generated file - rebuild with `npm run gazetteer:build` in functions/.
functions/src/data/kapellen-gazetteer.json

# Generated review file - rebuild with `npm run map:labels` in functions/.
functions/src/data/map-label-candidates.json

# Generated archive data - rebuild with `npm run archive:index` and `npm run stories`.
static/data/

# The old gzvka.be, saved exactly as it was served. Reformatting the source of the
# text migration would destroy the evidence it exists to preserve.
legacy-site/
docs/legacy-missing-photos.txt

# The researched places, and the file generated from them. The geojson is source data
# written elsewhere; reformatting 82 features would bury the next real change in noise.
functions/src/data/plaatsen.geojson
functions/src/data/place-approximations.json
